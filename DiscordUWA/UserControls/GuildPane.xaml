<UserControl
    x:Class="DiscordUWA.UserControls.GuildPane"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:uc="using:DiscordUWA.UserControls"
    xmlns:data="using:DiscordUWA.Models"
    xmlns:Converters="using:DiscordUWA.Converters"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="800"
    x:Name="root" Width="Auto">
    <UserControl.Resources>
        <Converters:StringExistsToVisibilityConverter x:Key="StringExistsToVisibilityConverter"/>
        <Converters:BoolToVisibilityConverter x:Key="BoolToVis"/>
        <Converters:ColorToSolidColorBrushValueConverter x:Key="ColorToBrush"/>
        <DataTemplate x:Key="ChatTextListTemplate" x:DataType="data:ChatTextListModel">
            <Grid VerticalAlignment="Bottom">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="50"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <TextBlock Text="{x:Bind TimeSent}" HorizontalAlignment="Right" Foreground="#FF686A6E" FontSize="12"/>
                <TextBlock Grid.Column="1" TextWrapping="WrapWholeWords">
                    <Run Text="{x:Bind Username}" Foreground="{x:Bind UserRoleColor, Converter={StaticResource ColorToBrush}}" FontWeight="Bold"/>
                    <Run Text="{x:Bind ChatText}"  />
                </TextBlock>
                <uc:GifView Grid.Column="1" Grid.Row="1" Height="100" Width="100" Stretch="Uniform" 
                            UriSource="{x:Bind Path=ImageUrl}" 
                            Visibility="{x:Bind Path=ImageUrl, Converter={StaticResource StringExistsToVisibilityConverter}}"/>

            </Grid>
        </DataTemplate>
    </UserControl.Resources>
    <Grid x:Name="chatGrid" Background="#FF36393E" Margin="0,0,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <ToggleButton x:Name="TogglePaneButton"
                          TabIndex="1"
                          Style="{StaticResource SplitViewTogglePaneButtonStyle}"
                          IsChecked="{x:Bind IsPaneOpen Mode=TwoWay}"
                          AutomationProperties.Name="Menu"
                          ToolTipService.ToolTip="Menu" Grid.Row="0" Grid.Column="0" />

            <StackPanel Grid.Column="1" VerticalAlignment="Center" ScrollViewer.VerticalScrollBarVisibility="Disabled" Orientation="Horizontal">
                <TextBlock HorizontalAlignment="Left" VerticalAlignment="Center" Text="#" Margin="20,0,0,0"/>
                <TextBlock Text="{x:Bind ChannelName, Mode=OneWay}" HorizontalAlignment="Left" VerticalAlignment="Center" FontWeight="Bold" />
                <TextBlock Text="{x:Bind ChannelTopic, Mode=OneWay}" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="20,0,0,0" />
            </StackPanel>
            <Button Command="{x:Bind ToggleUserListCommand}" Grid.Column="2" FontFamily="Segoe MDL2 Assets" Content="&#xE716;" Margin="20,0" HorizontalAlignment="Center" Background="Transparent"/>
        </Grid>
        <Grid Background="#FF252526" Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="90"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <ListView x:Name="chatLogListView" RenderTransformOrigin="0.5,0.5" Background="#FF36393E" ItemsSource="{x:Bind ChatLogList, Mode=OneWay}" ItemTemplate="{StaticResource ChatTextListTemplate}">
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsStackPanel ItemsUpdatingScrollMode="KeepLastItemInView" VerticalAlignment="Bottom"/>
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>
            </ListView>
            <Grid Grid.Row="1" VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox TextWrapping="Wrap" Text="{x:Bind MessageText, Mode=TwoWay}" PlaceholderText="Type Message..." Margin="20,0,0,0"/>
                <Button x:Name="sendChatMessage" Content="Send" Margin="20,0" Grid.Column="1" Command="{x:Bind SendMessageCommand}"/>
            </Grid>
            <SplitView x:Name="UserListSplitView" Visibility="{x:Bind ShowUserList, Mode=OneWay, Converter={StaticResource BoolToVis}, FallbackValue='Visible'}" IsPaneOpen="True" PanePlacement="Right" OpenPaneLength="300" HorizontalAlignment="Right" Grid.Column="1" Grid.RowSpan="2" DisplayMode="Inline">
                <SplitView.Pane>
                    <uc:UserList 
                        ShowChannelTopic="False"
                        ChannelTopic="{x:Bind ChannelTopic, Mode=OneWay}"
                        FullUserList="{x:Bind FullUserList, Mode=OneWay}" 
                        UserClick="{x:Bind UserClick}"
                        Margin="20,20,0,0" 
                        VerticalContentAlignment="Stretch" VerticalAlignment="Top"/>
                </SplitView.Pane>
            </SplitView>
        </Grid>
    </Grid>
</UserControl>